---
const { tag } = Astro.props
---

<a
    href={`/tags/${tag}`}
    class="text-gray-500 dark:text-neutral-400 text-xs font-medium underline hover:text-gray-700"
    title={`Explore posts tagged with #${tag}`}
    rel="noopener noreferrer"
>
    #{tag}
</a>
